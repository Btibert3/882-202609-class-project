version: 2

sources:
  - name: autoelite_raw
    database: "{{ env_var('GCP_PROJECT') }}"
    schema: autoelite_raw
    tables:
      - name: customers
      - name: reps
      - name: products
      - name: orders
      - name: order_items
      - name: deals   # populated by pipeline_deals — built in the Session 3 exercise

